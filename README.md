Installation

```shell
pip install cloxz-cli
```

Test whether it works

```shell
cxz --help
```

Start clocking

```shell
cxz [in / out] [text]
```

(optional) Add to your shell configuration

```shell
cxz status
```
